{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "marimo run marimo/aei_app.py --host 0.0.0.0 --port $PORT --no-token",
    "healthcheckPath": "/health"
  }
}